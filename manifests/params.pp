# Zotonic parameters
class zotonic::params
{
  $dir         = '/opt/zotonic'
  $modules_dir = "${dir}/user/modules"
  $sites_dir   = "${dir}/user/sites"
  $timezone    = 'UTC'
  $user        = 'zotonic'
}
